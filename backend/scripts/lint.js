const fs=require('fs'),path=require('path'); let failed=false;
function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){if(['node_modules','uploads','storage'].includes(e.name))continue;const p=path.join(dir,e.name);if(e.isDirectory())walk(p);else if(e.name.endsWith('.js'))try{require('child_process').execFileSync(process.execPath,['--check',p],{stdio:'pipe'})}catch(err){failed=true;console.error(err.stderr.toString())}}}
for(const target of ['app.js','server-mongodb.js','config','utils','models/mongo','services/applicationService.js','routes/v1.js','middleware/mongoAuth.js','scripts/migrate-mysql-to-mongodb.js','scripts/migrate-linked-mysql-to-mongodb.js','scripts/initialize-mongodb.js','scripts/verify-mongodb.js','scripts/verify-migration-relations.js','scripts/inspect-mysql-schema.js','scripts/create-super-admin-mongodb.js']) walkTarget(path.join(__dirname,'..',target));
function walkTarget(target){const stat=fs.statSync(target);if(stat.isDirectory())walk(target);else try{require('child_process').execFileSync(process.execPath,['--check',target],{stdio:'pipe'})}catch(err){failed=true;console.error(err.stderr.toString())}}
if(failed)process.exit(1); console.log('Syntaxe du backend MongoDB valide (routes MySQL historiques exclues)');
